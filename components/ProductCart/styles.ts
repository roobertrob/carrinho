const divClasses = ["m-3 ", "p-1", "hover:opacity-80", "text-center"].join(" ");

const ulClasses = [
  "w-full",
  "bg-[#4f4f5278]",
  "border",
  "rounded-lg",
  "flex",
  "justify-center",
  "items-center",
].join(" ");

const liImgClasses = ["flex", "justify-center"].join(" ");
const imgClasses = ["rounded-full", "m-2", "w-1/3"].join(" ");
const valueClasses = ["w-1/3"].join(" ");
const buttonClasses = ["flex", "items-center", "m-4"].join(" ");
const svgClasses = ["w-6"].join(" ");

export {
  divClasses,
  ulClasses,
  liImgClasses,
  imgClasses,
  valueClasses,
  buttonClasses,
  svgClasses,
};
