const divClasses = [
  "flex",
  "content-center",
  "justify-between",
  "h-[10vh]",
  "bg-[#4f4f52]",
  "text-white",
].join(" ");

const buttonClasses = ["font-extrabold", "text-3xl", "m-6", "mt-4"].join(" ");
const divLineClasses = ["bg-white", " w-full", "h-px", "m-auto"].join(" ");
const svgClasses = [
  "h-8 ",
  "w-8",
  "hover:text-gray-300",
  "cursor-pointer",
  "m-6",
  "ml-1",
].join(" ");

export { divClasses, buttonClasses, divLineClasses, svgClasses };
