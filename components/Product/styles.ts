const divProductClasses = [
  "flex",
  "flex-col",
  "justify-around",
  "items-center",
  "h-auto",
  "w-auto",
  "m-10",
  "border-2",
  "bg-[#4f4f5278]",
  "rounded-lg",
  "hover:scale-105",
  "delay-150",
  "shadow-lg",
].join(" ");

const divProductNameClasses = ["p-2"].join(" ");

const imgClasses = ["rounded-lg"].join(" ");

const valueClasses = ["p-2", "font-extrabold"].join(" ");

const buttonClasses = ["border-2", "p-2", "m-5", "rounded-lg"].join(" ");

export {
  divProductClasses,
  divProductNameClasses,
  imgClasses,
  valueClasses,
  buttonClasses,
};
